<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roomdatabse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoomDatabse">
        <activity android:name=".updateActivity"></activity>
        <activity android:name=".typeDetalisActivity" />
        <activity android:name=".typeActivity" />
        <activity android:name=".detailsActivity" />
        <activity android:name=".insertActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>